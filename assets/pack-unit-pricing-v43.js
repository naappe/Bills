(()=>{'use strict';
const VERSION=54;
const num=v=>Number(String(v??0).replace(/,/g,'').match(/-?\d+(?:\.\d+)?/)?.[0]||0);
const units=['PCS','BTL','ML','L','KG','G','CTN','DOZ','PKT'];
const directMeasures=new Set(['G','KG','L','ML','PCS']);
const containers=new Set(['CTN','DOZ','PKT','BTL']);
const moneyPrecise=v=>`MVR ${num(v).toLocaleString('en-US',{minimumFractionDigits:2,maximumFractionDigits:num(v)>0&&num(v)<.01?6:2})}`;
const normalize=u=>({KGS:'KG',KILOGRAM:'KG',KILOGRAMS:'KG',GRAM:'G',GRAMS:'G',LTR:'L',LTRS:'L',LITRE:'L',LITRES:'L',LITER:'L',LITERS:'L',MILLILITRE:'ML',MILLILITRES:'ML',MILLILITER:'ML',MILLILITERS:'ML',PC:'PCS',PIECE:'PCS',PIECES:'PCS',BOTTLE:'BTL',BOTTLES:'BTL',CARTON:'CTN',CARTONS:'CTN',PACKET:'PKT',PACKETS:'PKT',DOZEN:'DOZ'})[String(u||'').toUpperCase()]||String(u||'').toUpperCase();
function parsePack(value,unit){
 const purchase=normalize(unit||'PCS'),original=String(value||'').trim(),raw=original.toLowerCase().replace(/×/g,'x').replace(/,/g,'.').replace(/\s+/g,'');
 let count=1,size=1,measure=purchase,valid=false;
 const p='kg|kgs|kilograms?|g|grams?|l|ltr|ltrs|litres?|liters?|ml|millilitres?|milliliters?|pcs?|pieces?';
 const multi=raw.match(new RegExp(`^(\\d+(?:\\.\\d+)?)x(\\d+(?:\\.\\d+)?)(${p})$`,'i'));
 const single=raw.match(new RegExp(`^(\\d+(?:\\.\\d+)?)(${p})$`,'i'));
 if(multi){count=num(multi[1]);size=num(multi[2]);measure=normalize(multi[3]);valid=true}
 else if(single){size=num(single[1]);measure=normalize(single[2]);valid=true}
 else if(!original&&directMeasures.has(purchase)){measure=purchase;valid=true}
 const requiresPack=containers.has(purchase);
 let category='count',smallUnit=measure,smallPerPurchase=count*size,practicalUnit=measure,practicalPerPurchase=smallPerPurchase;
 if(measure==='KG'){category='weight';smallUnit='G';smallPerPurchase=count*size*1000;practicalUnit='KG';practicalPerPurchase=count*size}
 else if(measure==='G'){category='weight';smallUnit='G';practicalUnit='KG';practicalPerPurchase=count*size/1000}
 else if(measure==='L'){category='liquid';smallUnit='ML';smallPerPurchase=count*size*1000;practicalUnit='L';practicalPerPurchase=count*size}
 else if(measure==='ML'){category='liquid';smallUnit='ML';practicalUnit='L';practicalPerPurchase=count*size/1000}
 else if(measure==='PCS'){smallUnit='PCS';practicalUnit='PCS';practicalPerPurchase=count*size}
 const detected=valid?`${count} × ${size} ${measure} = ${practicalPerPurchase.toLocaleString('en-US',{maximumFractionDigits:3})} ${practicalUnit} per ${purchase}`:`Enter pack format, e.g. 24x500g or 12x1L`;
 return{purchase,original,count,size,measure,category,smallUnit,smallPerPurchase,practicalUnit,practicalPerPurchase,valid,requiresPack,detected};
}
function calc(item){
 const qty=Math.max(0,num(item.qty)),gst=Math.max(0,num(item.gst)),pack=parsePack(item.pack_format,item.unit),enteredRate=Math.max(0,num(item.rate)),enteredNet=Math.max(0,num(item.net_amount));
 const subtotal=enteredNet>0?enteredNet:qty*enteredRate,source=enteredNet>0?'net':'rate',gstAmount=subtotal*gst/100,total=subtotal+gstAmount,smallQty=qty*pack.smallPerPurchase,practicalQty=qty*pack.practicalPerPurchase;
 return{...pack,qty,gst,source,subtotal,gstAmount,total,smallQty,practicalQty,purchaseRate:qty?subtotal/qty:0,smallRate:smallQty?subtotal/smallQty:0,practicalRate:practicalQty?subtotal/practicalQty:0};
}
const qtyText=c=>`${c.practicalQty.toLocaleString('en-US',{maximumFractionDigits:3})} ${c.practicalUnit}`;
let frame=0;
function updateSummary(){
 const values=state.items.map(calc),sub=values.reduce((s,x)=>s+x.subtotal,0),gst=values.reduce((s,x)=>s+x.gstAmount,0);
 const items=$('#sumItems'),subtotal=$('#sumSubtotal'),gstEl=$('#sumGst'),grand=$('#sumGrand'),row=$('#sumGstRow');
 if(items)items.textContent=state.items.filter(i=>String(i.description||'').trim()||num(i.rate)||num(i.net_amount)).length;
 if(subtotal)subtotal.textContent=money(sub);if(gstEl)gstEl.textContent=money(gst);if(grand)grand.textContent=money(sub+gst);if(row)row.style.display=gst>0?'flex':'none';
}
function scheduleUpdate(i){cancelAnimationFrame(frame);frame=requestAnimationFrame(()=>{updateCard(i);updateSummary()})}
function updateCard(i){
 const card=document.querySelector(`.bill-item-card[data-index="${i}"]`);if(!card)return;const c=calc(state.items[i]);
 const set=(selector,value)=>{const el=card.querySelector(selector);if(el&&el.textContent!==value)el.textContent=value};
 set('[data-out="qty"]',qtyText(c));set('[data-label="purchase"]',`Rate per ${c.purchase}`);set('[data-out="purchase"]',`${moneyPrecise(c.purchaseRate)} / ${c.purchase}`);set('[data-out="line"]',money(c.total));
 const base=card.querySelector('[data-card="base"]');
 const showBase=c.practicalUnit!==c.purchase||c.smallUnit!==c.purchase;
 if(base){base.hidden=!showBase;if(showBase){const useSmall=c.smallUnit!==c.purchase&&c.smallUnit!==c.practicalUnit;set('[data-label="base"]',`Rate per ${useSmall?c.smallUnit:c.practicalUnit}`);set('[data-out="base"]',`${moneyPrecise(useSmall?c.smallRate:c.practicalRate)} / ${useSmall?c.smallUnit:c.practicalUnit}`)}}
 const help=card.querySelector('[data-pack-analysis]');if(help){help.textContent=c.detected;help.classList.toggle('invalid',!c.valid)}
}
function ensureStyle(){let s=document.querySelector('#packUnitPricingStyle');if(!s){s=document.createElement('style');s.id='packUnitPricingStyle';document.head.appendChild(s)}s.textContent=`.bill-item-fields{grid-template-columns:minmax(220px,2fr) 90px 110px minmax(170px,1.4fr) 120px 130px 90px!important}.bill-item-calcs{grid-template-columns:repeat(4,minmax(0,1fr))!important}.calc-card[hidden]{display:none!important}.calc-card.purchase{background:#edf8f3;border-color:#cceadd}.calc-card.base{background:#f2f7ff;border-color:#d8e7ff}.pack-analysis{color:#16734b!important;font-weight:700!important}.pack-analysis.invalid{color:#b45309!important}.new-layout{grid-template-columns:minmax(0,1fr)!important;max-width:100%!important}.new-layout>.summary{position:static!important;width:100%!important;max-width:none!important;display:grid!important;grid-template-columns:repeat(4,minmax(0,1fr)) minmax(150px,220px)!important;gap:10px!important}.new-layout>.summary h3,.new-layout>.summary #saveNotice{grid-column:1/-1!important}@media(max-width:1100px){.bill-item-fields{grid-template-columns:repeat(3,minmax(0,1fr))!important}.bill-item-fields .item-field:first-child{grid-column:span 3}.bill-item-calcs{grid-template-columns:repeat(2,minmax(0,1fr))!important}}@media(max-width:760px){.bill-item-fields{grid-template-columns:1fr 1fr!important}.bill-item-fields .item-field:first-child,.bill-item-fields .pack-field{grid-column:1/-1!important}.bill-item-calcs,.new-layout>.summary{grid-template-columns:1fr 1fr!important}}`}
window.renderItemRows=function(){
 ensureStyle();const root=$('#itemRows');if(!root)return;
 root.innerHTML=state.items.map((it,i)=>{const c=calc(it),purchase=normalize(it.unit||'PCS');return`<article class="bill-item-card" data-index="${i}"><div class="bill-item-head"><strong>Item ${i+1}</strong><button class="btn danger small" type="button" data-remove="${i}">Remove</button></div><div class="bill-item-fields"><label class="item-field"><span>Product</span><input class="field" data-i="${i}" data-k="description" value="${esc(it.description||'')}" placeholder="Product name"></label><label class="item-field"><span>Qty</span><input class="field" data-i="${i}" data-k="qty" type="number" min="0.001" step="any" value="${num(it.qty)}"></label><label class="item-field"><span>Unit</span><select class="field" data-i="${i}" data-k="unit">${units.map(x=>`<option ${purchase===x?'selected':''}>${x}</option>`).join('')}</select></label><label class="item-field pack-field"><span>Pack format</span><input class="field" data-i="${i}" data-k="pack_format" value="${esc(it.pack_format||'')}" placeholder="24x500g"><span class="item-help pack-analysis${c.valid?'':' invalid'}" data-pack-analysis>${esc(c.detected)}</span></label><label class="item-field"><span>Rate</span><input class="field" data-i="${i}" data-k="rate" type="number" min="0" step="any" value="${num(it.rate)}" placeholder="Optional"></label><label class="item-field"><span>Net amount</span><input class="field" data-i="${i}" data-k="net_amount" type="number" min="0" step="any" value="${num(it.net_amount)}" placeholder="Optional"></label><label class="item-field"><span>GST %</span><input class="field" data-i="${i}" data-k="gst" type="number" min="0" step="any" value="${num(it.gst)}"></label></div><div class="bill-item-calcs"><div class="calc-card total"><small>Total quantity</small><strong data-out="qty">${qtyText(c)}</strong></div><div class="calc-card purchase"><small data-label="purchase">Rate per ${c.purchase}</small><strong data-out="purchase">${moneyPrecise(c.purchaseRate)} / ${c.purchase}</strong></div><div class="calc-card base" data-card="base" ${c.practicalUnit===c.purchase&&c.smallUnit===c.purchase?'hidden':''}><small data-label="base">Base rate</small><strong data-out="base">${moneyPrecise(c.smallRate)} / ${c.smallUnit}</strong></div><div class="calc-card line"><small>Line total</small><strong data-out="line">${money(c.total)}</strong></div></div></article>`}).join('');
 root.oninput=e=>{const el=e.target.closest('[data-i][data-k]');if(!el)return;const i=+el.dataset.i,k=el.dataset.k;state.items[i][k]=['qty','rate','net_amount','gst'].includes(k)?num(el.value):el.value;scheduleUpdate(i)};
 root.onchange=e=>{const el=e.target.closest('[data-i][data-k]');if(!el)return;const i=+el.dataset.i,k=el.dataset.k;state.items[i][k]=['qty','rate','net_amount','gst'].includes(k)?num(el.value):el.value;scheduleUpdate(i)};
 root.onclick=e=>{const b=e.target.closest('[data-remove]');if(!b)return;state.items.splice(+b.dataset.remove,1);if(!state.items.length)state.items=[window.freshItem()];window.renderItemRows()};
 updateSummary();
};
window.saveBill=async function(e){e.preventDefault();const notice=$('#saveNotice'),save=$('#saveBillBtn'),valid=state.items.filter(i=>String(i.description||'').trim()&&num(i.qty)>0);if(!valid.length){notice.textContent='Add at least one valid item.';return}const missingPrice=valid.find(i=>!num(i.rate)&&!num(i.net_amount));if(missingPrice){notice.textContent='Enter either Rate or Net Amount for every item.';return}const invalid=valid.find(i=>{const c=calc(i);return c.requiresPack&&!c.valid});if(invalid){notice.textContent='Enter a valid pack format for carton, dozen, packet or bottle items.';return}save.disabled=true;save.textContent='Saving…';const f=new FormData(e.currentTarget),items=valid.map(i=>{const c=calc(i);return{...i,inner_unit:null,rate_basis:'purchase',purchase_unit:c.purchase,pricing_source:c.source,effective_rate:c.purchaseRate,net_amount:c.subtotal,pack_category:c.category,pack_count:c.count,pack_size:c.size,pack_measure:c.measure,purchase_rate:c.purchaseRate,practical_unit:c.practicalUnit,practical_quantity:c.practicalQty,practical_rate:c.practicalRate,base_unit:c.smallUnit,base_quantity:c.smallQty,base_rate:c.smallRate,line_total:c.total,gst_amount:c.gstAmount}}),sub=items.reduce((s,i)=>s+(num(i.line_total)-num(i.gst_amount)),0),gstTotal=items.reduce((s,i)=>s+num(i.gst_amount),0),payload={vendor:f.get('vendor'),bill_date:f.get('bill_date'),bill_no:f.get('bill_no')||'',tin:f.get('tin')||'',location:f.get('location')||'',payment_status:f.get('payment_status'),payment_method:f.get('payment_method')||'',notes:f.get('notes')||'',amount:sub+gstTotal,subtotal:sub,gst_total:gstTotal,items};let q=state.editing?db.from(TABLE).update(payload).eq('id',state.editing.id):db.from(TABLE).insert(payload);let{error}=await q;if(error){const safe={vendor:payload.vendor,bill_date:payload.bill_date,amount:payload.amount,payment_status:payload.payment_status,payment_method:payload.payment_method,notes:payload.notes};({error}=await(state.editing?db.from(TABLE).update(safe).eq('id',state.editing.id):db.from(TABLE).insert(safe)))}if(error){notice.textContent=error.message;save.disabled=false;save.textContent=state.editing?'Update Bill':'Save Bill';return}state.editing=null;state.items=[];await loadBills();show('bills')};
const oldRender=window.renderNewBill;if(typeof oldRender==='function'){window.renderNewBill=function(){oldRender.apply(this,arguments);window.renderItemRows();ensureStyle()}}ensureStyle();if(location.hash==='#new')requestAnimationFrame(()=>window.renderItemRows());window.__WS_PACK_UNIT_PRICING__={version:VERSION,calc,parsePack};})();