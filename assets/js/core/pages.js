(()=>{
'use strict';
const VERSION=1;
const byId=id=>document.getElementById(id);
const rows=()=>Array.isArray(state.rows)?state.rows:[];
const val=(row,...keys)=>{for(const key of keys){if(row&&row[key]!==undefined&&row[key]!==null)return row[key]}return''};
const text=v=>String(v??'').trim();
const amount=row=>Number(window.amountVal?.(row)||0);
const vendor=row=>text(window.vendorVal?.(row));
const status=row=>text(window.statusVal?.(row)||'Pending');
const billNo=row=>text(val(row,'bill_no','Bill No'))||'-';
const addedAt=row=>val(row,'created_at');
const billDay=row=>{const raw=val(row,'bill_day','bill_date','Bill Date','date','Date');if(!raw)return'';const s=String(raw).slice(0,10);if(/^\d{4}-\d{2}-\d{2}$/.test(s))return s;const d=new Date(raw);return Number.isNaN(d.getTime())?'':`${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,'0')}-${String(d.getDate()).padStart(2,'0')}`};
const longDate=raw=>{if(!raw)return'-';const s=String(raw).slice(0,10),m=s.match(/^(\d{4})-(\d{2})-(\d{2})$/),d=m?new Date(+m[1],+m[2]-1,+m[3]):new Date(raw);return Number.isNaN(d.getTime())?String(raw):d.toLocaleDateString('en-GB',{day:'numeric',month:'long',year:'numeric'})};
const dateTime=raw=>{if(!raw)return'-';const d=new Date(raw);return Number.isNaN(d.getTime())?'-':d.toLocaleString('en-GB',{day:'numeric',month:'short',year:'numeric',hour:'2-digit',minute:'2-digit'})};
const localDay=(d=new Date())=>`${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,'0')}-${String(d.getDate()).padStart(2,'0')}`;
const range=preset=>{const now=new Date(),today=localDay(now);let start='',end='';if(preset==='today')start=end=today;if(preset==='week'){const d=new Date(now);d.setHours(0,0,0,0);d.setDate(d.getDate()-((d.getDay()+6)%7));start=localDay(d);end=today}if(preset==='month'){start=`${today.slice(0,7)}-01`;end=today}if(preset==='lastmonth'){start=localDay(new Date(now.getFullYear(),now.getMonth()-1,1));end=localDay(new Date(now.getFullYear(),now.getMonth(),0))}if(preset==='year'){start=`${today.slice(0,4)}-01-01`;end=today}return{start,end}};
const filterDate=(list,preset,from='',to='')=>{let{start,end}=range(preset);if(preset==='custom'){start=from;end=to}return list.filter(row=>{const day=billDay(row);return(!start||day>=start)&&(!end||day<=end)})};
const itemList=()=>{const out=[];rows().forEach(bill=>(Array.isArray(bill.items)?bill.items:[]).forEach(item=>out.push({bill,item})));return out};
const itemName=item=>text(val(item,'product','item_name','description','name','item'));
const itemRate=item=>Number(val(item,'rate','unit_rate','pack_rate')||0);
const itemQty=item=>Number(val(item,'qty','quantity')||0);
const kpi=(label,value,sub='',tone='blue',canvas='')=>`<article class="kpi-card tone-${tone}"><div class="kpi-top"><div><div class="kpi-label">${esc(label)}</div><div class="kpi-value">${value}</div></div>${sub?`<span class="kpi-badge">${esc(sub)}</span>`:''}</div>${canvas?`<div class="kpi-mini"><canvas id="${canvas}"></canvas></div>`:''}</article>`;
const destroyCharts=()=>{(window.__WS_PAGE_CHARTS__||[]).forEach(chart=>chart?.destroy?.());window.__WS_PAGE_CHARTS__=[]};
const addChart=chart=>{window.__WS_PAGE_CHARTS__=window.__WS_PAGE_CHARTS__||[];window.__WS_PAGE_CHARTS__.push(chart)};
const makeLine=(id,data,color='#0066ff')=>{const el=byId(id);if(!el||typeof Chart==='undefined')return;addChart(new Chart(el,{type:'line',data:{labels:data.map((_,i)=>i+1),datasets:[{data,borderColor:color,backgroundColor:`${color}20`,fill:true,borderWidth:2,tension:.35,pointRadius:0}]},options:{responsive:true,maintainAspectRatio:false,plugins:{legend:{display:false},tooltip:{enabled:false}},scales:{x:{display:false},y:{display:false}},animation:{duration:550}}}))};
const userMap=async()=>{const map=new Map();const{data}=await db.from('user_presence').select('user_id,email,display_name');(data||[]).forEach(u=>map.set(String(u.user_id),u.display_name||u.email||String(u.user_id).slice(0,8)));return map};

window.renderDashboard=async()=>{
 destroyCharts();
 const list=rows(),latest=[...list].sort((a,b)=>String(addedAt(b)||'').localeCompare(String(addedAt(a)||'')),todayRows=filterDate(list,'today'),monthRows=filterDate(list,'month'),outstanding=list.filter(r=>status(r).toLowerCase()!=='paid').reduce((s,r)=>s+amount(r),0);
 const previousMonth=(()=>{const now=new Date(),key=`${now.getFullYear()}-${String(now.getMonth()).padStart(2,'0')}`;return list.filter(r=>billDay(r).startsWith(key)).reduce((s,r)=>s+amount(r),0)})();
 const monthTotal=monthRows.reduce((s,r)=>s+amount(r),0),change=previousMonth?((monthTotal-previousMonth)/previousMonth*100):0;
 const daily=new Map();for(let i=13;i>=0;i--){const d=new Date();d.setDate(d.getDate()-i);daily.set(localDay(d),0)}monthRows.forEach(r=>{const d=billDay(r);if(daily.has(d))daily.set(d,daily.get(d)+amount(r))});
 const vendors=new Map();list.forEach(r=>vendors.set(vendor(r)||'Unknown',(vendors.get(vendor(r)||'Unknown')||0)+amount(r)));const ranked=[...vendors].sort((a,b)=>b[1]-a[1]).slice(0,8),max=ranked[0]?.[1]||1;
 byId('content').innerHTML=pageHead('Dashboard','Live procurement performance',state.role!=='readonly'?'<button class="btn" data-go="new">New Bill</button>':'')+
 `<section class="kpi-grid">${kpi('Today bills',todayRows.length.toLocaleString(),'Today','blue','kpiToday')}${kpi('This month',money(monthTotal),`${change>=0?'↑':'↓'} ${Math.abs(change).toFixed(1)}%`,'purple','kpiMonth')}${kpi('Outstanding',money(outstanding),'Unpaid','orange','kpiOutstanding')}${kpi('Last added',dateTime(addedAt(latest[0])),'Latest','green')}</section>`+
 `<section class="dashboard-layout"><article class="card chart-card"><div class="card-head"><div><strong>Purchase trend</strong><div class="muted">Last 14 days</div></div></div><div class="main-chart"><canvas id="purchaseTrend"></canvas></div></article><article class="card kpi-rank"><div class="card-head"><strong>Top vendors</strong></div><div class="rank-list">${ranked.map(([name,total],i)=>`<div class="rank-item"><span class="rank-no">#${i+1}</span><span class="rank-name">${esc(name)}</span><div class="bar-track"><span class="bar-fill" style="width:${Math.max(4,total/max*100)}%"></span></div><strong>${money(total)}</strong></div>`).join('')||'<div class="empty">No vendor data</div>'}</div></article></section>`+
 `<section class="card"><div class="card-head"><strong>Latest added bills</strong><button class="btn secondary small" data-go="bills">View all</button></div><div class="table-wrap"><table><thead><tr><th>Added at</th><th>Bill date</th><th>Vendor</th><th>Bill no.</th><th>Status</th><th>Amount</th></tr></thead><tbody>${latest.slice(0,20).map(r=>`<tr><td>${esc(dateTime(addedAt(r)))}</td><td>${esc(longDate(billDay(r)))}</td><td>${esc(vendor(r)||'-')}</td><td>${esc(billNo(r))}</td><td><span class="pill">${esc(status(r))}</span></td><td>${money(amount(r))}</td></tr>`).join('')}</tbody></table></div></section>`;
 requestAnimationFrame(()=>{makeLine('kpiToday',Array.from({length:8},(_,i)=>Math.max(0,todayRows.length-4+i%3)),'#0066ff');makeLine('kpiMonth',Array.from(daily.values()).slice(-8),'#7c5cff');makeLine('kpiOutstanding',latest.slice(0,8).reverse().map(r=>amount(r)),'#f0a33a');const el=byId('purchaseTrend');if(el&&typeof Chart!=='undefined')addChart(new Chart(el,{type:'bar',data:{labels:[...daily.keys()].map(d=>new Date(`${d}T00:00:00`).toLocaleDateString('en-GB',{day:'2-digit',month:'short'})),datasets:[{data:[...daily.values()],backgroundColor:'rgba(0,102,255,.22)',borderColor:'#0066ff',borderWidth:1.5,borderRadius:7}]},options:{responsive:true,maintainAspectRatio:false,plugins:{legend:{display:false}},scales:{x:{grid:{display:false}},y:{beginAtZero:true,grid:{color:'rgba(19,45,75,.07)'},ticks:{callback:v=>`MVR ${Number(v).toLocaleString()}`}}}}}))});
};

window.renderBills=async()=>{
 destroyCharts();const list=rows(),users=await userMap();state.filtered=[...list];state.page=1;
 byId('content').innerHTML=pageHead('Bills',`${list.length.toLocaleString()} active records`,`<button class="btn secondary" id="exportBills">Export CSV</button>${state.role!=='readonly'?'<button class="btn" data-go="new">New Bill</button>':''}`)+`<section class="kpi-grid" id="billMetrics"></section><section class="card"><div class="toolbar bills-toolbar"><input class="field" id="billSearch" placeholder="Search vendor or bill no."><select class="field" id="datePreset"><option value="all" selected>All dates</option><option value="today">Today</option><option value="week">This week</option><option value="month">This month</option><option value="lastmonth">Last month</option><option value="year">This year</option><option value="custom">Date range</option></select><select class="field" id="statusFilter"><option value="">All statuses</option>${[...new Set(list.map(status))].sort().map(s=>`<option>${esc(s)}</option>`).join('')}</select><select class="field" id="pageSize"><option>20</option><option>50</option><option>100</option></select><input class="field hidden" id="dateFrom" type="date"><input class="field hidden" id="dateTo" type="date"></div><div class="table-wrap"><table><thead><tr><th>Status</th><th>Bill date</th><th>Added at</th><th>Bill no.</th><th>Vendor</th><th>Amount</th><th>Added by</th><th>Actions</th></tr></thead><tbody id="billRows"></tbody></table></div><div class="pager" id="pager"></div></section>`;
 const render=()=>{const visible=state.filtered||[],size=Number(state.pageSize||20),pages=Math.max(1,Math.ceil(visible.length/size));state.page=Math.min(Math.max(1,state.page||1),pages);const start=(state.page-1)*size,latest=[...visible].sort((a,b)=>String(addedAt(b)||'').localeCompare(String(addedAt(a)||''))[0],paid=visible.filter(r=>status(r).toLowerCase()==='paid').reduce((s,r)=>s+amount(r),0);byId('billMetrics').innerHTML=kpi('Visible bills',visible.length.toLocaleString(),'Filtered','blue')+kpi('Visible value',money(visible.reduce((s,r)=>s+amount(r),0)),'Total','purple')+kpi('Paid value',money(paid),'Paid','green')+kpi('Latest added',dateTime(addedAt(latest)),'Newest','orange');byId('billRows').innerHTML=visible.slice(start,start+size).map(r=>{const uid=String(val(r,'user_id','updated_by')||''),who=users.get(uid)||text(val(r,'created_by'))||'-';return`<tr><td><span class="pill">${esc(status(r))}</span></td><td>${esc(longDate(billDay(r)))}</td><td>${esc(dateTime(addedAt(r)))}</td><td>${esc(billNo(r))}</td><td>${esc(vendor(r)||'-')}</td><td>${money(amount(r))}</td><td>${esc(who)}</td><td><div class="actions">${state.role!=='readonly'?`<button class="btn secondary small" data-edit="${r.id}">Edit</button>`:''}${state.role==='admin'?`<button class="btn danger small" data-delete="${r.id}">Delete</button>`:''}</div></td></tr>`}).join('')||'<tr><td colspan="8"><div class="empty">No bills found.</div></td></tr>';byId('billRows').querySelectorAll('[data-edit]').forEach(b=>b.onclick=()=>{state.editing=list.find(r=>String(r.id)===b.dataset.edit)||null;show('new')});byId('billRows').querySelectorAll('[data-delete]').forEach(b=>b.onclick=async()=>{if(!confirm('Delete this bill?'))return;b.disabled=true;const{error}=await db.from(TABLE).delete().eq('id',b.dataset.delete);if(error){alert(error.message);b.disabled=false;return}await reloadBillsNow()});byId('pager').innerHTML=`<span>${visible.length?start+1:0}-${Math.min(start+size,visible.length)} of ${visible.length}</span><div class="actions"><button class="btn secondary small" id="prev" ${state.page<=1?'disabled':''}>Previous</button><span>Page ${state.page} of ${pages}</span><button class="btn secondary small" id="next" ${state.page>=pages?'disabled':''}>Next</button></div>`;byId('prev').onclick=()=>{state.page--;render()};byId('next').onclick=()=>{state.page++;render()}};
 const apply=()=>{const q=text(byId('billSearch').value).toLowerCase(),preset=byId('datePreset').value,s=text(byId('statusFilter').value).toLowerCase();state.filtered=filterDate(list,preset,byId('dateFrom').value,byId('dateTo').value).filter(r=>(!q||`${vendor(r)} ${billNo(r)}`.toLowerCase().includes(q))&&(!s||status(r).toLowerCase()===s)).sort((a,b)=>String(addedAt(b)||'').localeCompare(String(addedAt(a)||'')));state.page=1;render()};
 byId('datePreset').onchange=()=>{const custom=byId('datePreset').value==='custom';byId('dateFrom').classList.toggle('hidden',!custom);byId('dateTo').classList.toggle('hidden',!custom);apply()};['billSearch','dateFrom','dateTo'].forEach(id=>byId(id).oninput=apply);byId('statusFilter').onchange=apply;byId('pageSize').value=String(state.pageSize||20);byId('pageSize').onchange=e=>{state.pageSize=Number(e.target.value)||20;render()};byId('exportBills').onclick=window.exportCsv;apply();
};

const originalNew=window.renderNewBill;
window.renderNewBill=async()=>{destroyCharts();await originalNew?.();if(!state.editing&&Array.isArray(state.items)&&state.items.length>1){const buttons=[...document.querySelectorAll('#itemRows [data-remove]')];for(let i=buttons.length-1;i>0;i--)buttons[i]?.click()}const form=byId('billForm'),head=byId('content')?.querySelector('.page-head');if(!form||!head)return;const panel=document.createElement('section');panel.className='kpi-grid';panel.id='newBillMetrics';head.insertAdjacentElement('afterend',panel);const update=()=>{const items=Array.isArray(state.items)?state.items:[],subtotal=items.reduce((s,i)=>s+Number(i.qty||0)*Number(i.rate||0),0),gst=items.reduce((s,i)=>s+Number(i.qty||0)*Number(i.rate||0)*Number(i.gst||0)/100,0);panel.innerHTML=kpi('Item rows',items.length.toLocaleString(),'Draft','blue')+kpi('Subtotal',money(subtotal),'Before GST','purple')+kpi('GST',money(gst),'Tax','orange')+kpi('Draft total',money(subtotal+gst),'Total','green')};update();form.addEventListener('input',()=>requestAnimationFrame(update));byId('addRow')?.addEventListener('click',()=>requestAnimationFrame(update));byId('itemRows')?.addEventListener('click',e=>{if(e.target.closest('[data-remove]'))requestAnimationFrame(update)})};

window.renderProducts=()=>{destroyCharts();const map=new Map();itemList().forEach(({bill,item})=>{const name=itemName(item);if(!name)return;const key=name.toLowerCase(),current=map.get(key)||{name,packing:'',unit:'',qty:0,total:0,rate:0,vendor:'',bill:'-',date:'',added:''};current.qty+=itemQty(item);current.total+=Number(val(item,'line_total','amount')||itemQty(item)*itemRate(item));if(!current.added||String(addedAt(bill))>String(current.added)){current.packing=text(val(item,'pack_format','packing'));current.unit=text(val(item,'unit','purchase_unit'));current.rate=itemRate(item);current.vendor=vendor(bill);current.bill=billNo(bill);current.date=billDay(bill);current.added=addedAt(bill)}map.set(key,current)});const list=[...map.values()].sort((a,b)=>String(b.added).localeCompare(String(a.added))),withRate=list.filter(p=>p.rate>0),total=list.reduce((s,p)=>s+p.total,0);byId('content').innerHTML=pageHead('Products',`${list.length.toLocaleString()} products from bill items`)+`<section class="kpi-grid">${kpi('Products',list.length.toLocaleString(),'Tracked','blue')}${kpi('With rates',withRate.length.toLocaleString(),'Priced','green')}${kpi('Purchase value',money(total),'All time','purple')}${kpi('Latest added',dateTime(list[0]?.added),'Newest','orange')}</section><section class="card"><div class="card-head"><strong>Product purchasing activity</strong></div><div class="table-wrap"><table><thead><tr><th>Product</th><th>Packing</th><th>Unit</th><th>Total quantity</th><th>Latest rate</th><th>Vendor</th><th>Bill</th><th>Bill date</th><th>Added at</th></tr></thead><tbody>${list.slice(0,300).map(p=>`<tr><td><strong>${esc(p.name)}</strong></td><td>${esc(p.packing||'-')}</td><td>${esc(p.unit||'-')}</td><td>${p.qty.toLocaleString()}</td><td>${money(p.rate)}</td><td>${esc(p.vendor||'-')}</td><td>${esc(p.bill)}</td><td>${esc(longDate(p.date))}</td><td>${esc(dateTime(p.added))}</td></tr>`).join('')||'<tr><td colspan="9"><div class="empty">No product activity found.</div></td></tr>'}</tbody></table></div></section>`};

window.renderVendors=async()=>{destroyCharts();const{data:profiles=[],error}=await db.from('vendors').select('id,name,tin,phone,email,address,is_active,created_at,updated_at').is('deleted_at',null).order('created_at',{ascending:false});const totals=new Map();rows().forEach(b=>{const name=vendor(b);if(!name)return;const key=name.toLowerCase(),current=totals.get(key)||{bills:0,total:0,latestAdded:'',latestDate:'',latestBill:'-'};current.bills++;current.total+=amount(b);if(!current.latestAdded||String(addedAt(b))>String(current.latestAdded)){current.latestAdded=addedAt(b);current.latestDate=billDay(b);current.latestBill=billNo(b)}totals.set(key,current)});const list=(error?[]:profiles).map(p=>({...p,summary:totals.get(text(p.name).toLowerCase())||{bills:0,total:0,latestAdded:'',latestDate:'',latestBill:'-'}})),complete=list.filter(v=>v.tin&&v.phone),purchase=list.reduce((s,v)=>s+v.summary.total,0);byId('content').innerHTML=pageHead('Vendors',`${list.length.toLocaleString()} vendor profiles`)+`<section class="kpi-grid">${kpi('Active vendors',list.filter(v=>v.is_active!==false).length.toLocaleString(),'Active','blue')}${kpi('Complete profiles',complete.length.toLocaleString(),'TIN + phone','green')}${kpi('Purchase value',money(purchase),'All time','purple')}${kpi('Latest vendor',dateTime(list[0]?.created_at),'Added','orange')}</section><section class="card"><div class="card-head"><strong>Vendor purchasing activity</strong></div><div class="table-wrap"><table><thead><tr><th>Vendor</th><th>TIN</th><th>Phone</th><th>Bills</th><th>Total value</th><th>Latest bill</th><th>Bill date</th><th>Added at</th></tr></thead><tbody>${list.map(v=>`<tr><td><strong>${esc(v.name)}</strong><div class="muted">${esc(v.email||v.address||'')}</div></td><td>${esc(v.tin||'-')}</td><td>${esc(v.phone||'-')}</td><td>${v.summary.bills}</td><td>${money(v.summary.total)}</td><td>${esc(v.summary.latestBill)}</td><td>${esc(longDate(v.summary.latestDate))}</td><td>${esc(dateTime(v.summary.latestAdded))}</td></tr>`).join('')||'<tr><td colspan="8"><div class="empty">No vendors found.</div></td></tr>'}</tbody></table></div></section>`};

window.renderPrices=()=>{destroyCharts();const entries=itemList().map(({bill,item})=>({name:itemName(item)||'-',packing:text(val(item,'pack_format','packing')),unit:text(val(item,'unit','purchase_unit')),rate:itemRate(item),smallRate:Number(val(item,'small_rate','base_unit_rate')||0),vendor:vendor(bill),bill:billNo(bill),date:billDay(bill),added:addedAt(bill)})).sort((a,b)=>String(b.added).localeCompare(String(a.added))),products=new Set(entries.map(e=>e.name.toLowerCase())).size,vendors=new Set(entries.map(e=>e.vendor.toLowerCase()).filter(Boolean)).size,avg=entries.length?entries.reduce((s,e)=>s+e.rate,0)/entries.length:0;byId('content').innerHTML=pageHead('Price Book',`${entries.length.toLocaleString()} bill-item rates`)+`<section class="kpi-grid">${kpi('Price records',entries.length.toLocaleString(),'Tracked','blue')}${kpi('Products',products.toLocaleString(),'Unique','green')}${kpi('Vendors',vendors.toLocaleString(),'Sources','purple')}${kpi('Average rate',money(avg),'Pack rate','orange')}</section><section class="card"><div class="card-head"><strong>Latest recorded prices</strong></div><div class="table-wrap"><table><thead><tr><th>Product</th><th>Packing</th><th>Unit</th><th>Pack rate</th><th>Base rate</th><th>Vendor</th><th>Bill</th><th>Bill date</th><th>Added at</th></tr></thead><tbody>${entries.slice(0,500).map(e=>`<tr><td><strong>${esc(e.name)}</strong></td><td>${esc(e.packing||'-')}</td><td>${esc(e.unit||'-')}</td><td>${money(e.rate)}</td><td>${money(e.smallRate)}</td><td>${esc(e.vendor||'-')}</td><td>${esc(e.bill)}</td><td>${esc(longDate(e.date))}</td><td>${esc(dateTime(e.added))}</td></tr>`).join('')||'<tr><td colspan="9"><div class="empty">No rates found.</div></td></tr>'}</tbody></table></div></section>`};

window.renderReports=()=>{destroyCharts();const list=rows(),now=new Date(),year=String(now.getFullYear()),month=`${year}-${String(now.getMonth()+1).padStart(2,'0')}`,yearRows=list.filter(r=>billDay(r).startsWith(year)),monthRows=list.filter(r=>billDay(r).startsWith(month)),latest=[...list].sort((a,b)=>String(addedAt(b)).localeCompare(String(addedAt(a)))).slice(0,20),paid=list.filter(r=>status(r).toLowerCase()==='paid').reduce((s,r)=>s+amount(r),0),outstanding=list.reduce((s,r)=>s+amount(r),0)-paid;byId('content').innerHTML=pageHead('Reports','Procurement performance and recent activity')+`<section class="kpi-grid">${kpi('This year',money(yearRows.reduce((s,r)=>s+amount(r),0)),'Year','blue')}${kpi('This month',money(monthRows.reduce((s,r)=>s+amount(r),0)),'Month','purple')}${kpi('Paid value',money(paid),'Paid','green')}${kpi('Outstanding',money(outstanding),'Due','orange')}</section><section class="card"><div class="card-head"><strong>Latest additions</strong></div><div class="table-wrap"><table><thead><tr><th>Added at</th><th>Bill date</th><th>Vendor</th><th>Bill no.</th><th>Status</th><th>Amount</th></tr></thead><tbody>${latest.map(r=>`<tr><td>${esc(dateTime(addedAt(r)))}</td><td>${esc(longDate(billDay(r)))}</td><td>${esc(vendor(r)||'-')}</td><td>${esc(billNo(r))}</td><td>${esc(status(r))}</td><td>${money(amount(r))}</td></tr>`).join('')}</tbody></table></div></section>`};

window.renderSettings=()=>{destroyCharts();const latest=[...rows()].sort((a,b)=>String(addedAt(b)).localeCompare(String(addedAt(a)))[0],dbStatus=window.__WS_DB_STATUS__?.status||'Unknown';byId('content').innerHTML=pageHead('Settings','Application and account status')+`<section class="kpi-grid">${kpi('Role',esc(state.role),'Access','blue')}${kpi('Active records',rows().length.toLocaleString(),'Bills','green')}${kpi('Database',esc(dbStatus),'Connection','purple')}${kpi('Last data added',dateTime(addedAt(latest)),'Latest','orange')}</section><section class="card"><div class="card-head"><strong>Account and system</strong></div><div class="card-body settings-grid"><div class="kpi"><small>Signed-in user</small><strong>${esc(state.user?.email||'-')}</strong></div><div class="kpi"><small>Current page</small><strong>${esc(state.view||'-')}</strong></div><div class="kpi"><small>UI version</small><strong>${esc(window.__WS_CORE__?.version||'-')}</strong></div><div class="kpi"><small>Pages version</small><strong>${VERSION}</strong></div></div></section>`};

Object.assign(window.__WS_RENDERERS__,{dashboard:window.renderDashboard,bills:window.renderBills,new:window.renderNewBill,products:window.renderProducts,vendors:window.renderVendors,prices:window.renderPrices,reports:window.renderReports,settings:window.renderSettings});
window.__WS_PAGES__={version:VERSION};
})();